# Project agent notes

Repository-wide agent instructions are defined in `../AGENTS.md`.

No project-specific reusable agent skills are required for this exercise. Add a
skill under `.agents/skills/<skill-name>/SKILL.md` only when a workflow will be
used repeatedly and the user explicitly asks to expand the agent toolkit.
